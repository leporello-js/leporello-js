name: Test

on: 
  push:
    branches: '*'

jobs:

  test:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v2

      - name: test
        run: |
          node test/run.js
          node test/self_hosted_test.js
